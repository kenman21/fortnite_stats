let defaultState = {
    players: [],
    average: {},
    name: "",
    platform: "",
    history: null
  }

export default function manageHistory(state = defaultState, action) {
  switch (action.type){
    default:
      return state
    }
  }
